What happens when you initialize a repository? Why do you need to do it?
It creates .git directory and prepares all files to be commited.
We need to initialize before we add files to track

How is the staging area different from the working directory and the repository? What value do you think it offers?

Staging area is sandbox where we chosse files to cmmit. The main value is that we can easily reset the list of files preapred to commit add/remove files as well

How can you use the staging area to make sure you have one commit per logical change?

I can add one file only to the staging area for example and the diff with stage 

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
It helps test some solutions, which we might or might not add to the general project.
Also help to test new features. Branches keep files in order, so we can come back to modify our main program code

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
Manual merge might not see all conflicts while autmatic merge helps you to do this.


